<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shivam.mobilelearning1">

    <uses-permission android:name="android.permission.INTERNET" />

    <service android:name=".MyFirebaseMessagingService">
        <intent-filter>
            <action android:name="com.google.firebase.MESSAGING_EVENT" />
        </intent-filter>
    </service>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".QuestionnaireScrollingActivity"
            android:label="@string/title_activity_questionnaire_scrolling"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".DisplayLearningStyleActivity" />
        <activity android:name=".GoogleSignInActivity" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".DashboardActivity"
            android:label="@string/title_activity_dashboard"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".DashProfileActivity"
            android:label="@string/title_activity_dash_profile"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".DashHomeActivity" />
        <activity android:name=".DashDiscoverActivity"></activity>
    </application>

</manifest>